services:
  vr2ar:
    # build:
    #  context: .
    image: "docker.io/mastertbo/vr2arconverterv3:multigpu"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    ports:
      - "7860:7860"
    ipc: host
